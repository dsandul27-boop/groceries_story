spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dorindb
    username: postgres
    password: sharp06
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
